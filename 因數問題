#include<stdio.h>    
int main(){    
    int i,a;    
    while(scanf("%d",&a)!=EOF){    
        printf("1");  
        for(i=2;i<=a;i++){  
            if(a%i==0){  
                printf(" %d",i);  
            }  
        }printf("\n");  
    }   
    return 0;  
}  
